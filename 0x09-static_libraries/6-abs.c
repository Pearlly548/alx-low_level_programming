#include "main.h"

/**
 * _abs - Entry point
 * @n: n is the integer
 * Description: prints the correct value of the integer
 * Return: int
 */

int _abs(int a)

	{
	if (a < 0)
		return (-a);
	else
		return (a);
}
